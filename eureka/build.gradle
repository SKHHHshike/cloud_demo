dependencies {
    compile('org.springframework.cloud:spring-cloud-starter-netflix-eureka-server')
    compile(group: 'net.logstash.logback', name: 'logstash-logback-encoder', version:'5.2')
//    comlile(group: 'org.codehaus.janino', name: 'janino')
}
